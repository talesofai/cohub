---
"@neta-art/cohub": minor
---

Generation model declarations can carry a display `pricing` block (`unit`, `amount` or `min`/`max`, optional `note`). `models.listMultimodal()` returns it with each model, and the web model picker renders it next to the model id.
