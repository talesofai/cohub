name: Gateway Docker Build and Push

on:
  workflow_call:
    inputs:
      source_sha:
        required: false
        type: string
      skip_preflight:
        required: false
        type: boolean
        default: false
  workflow_dispatch:

permissions:
  contents: read

env:
  REGISTRY: git.talesofai.com
  NAMESPACE: ${{ secrets.GITEA_OWNER }}
  IMAGE_NAME: cohub-gateway

jobs:
  build-and-push:
    if: ${{ !startsWith(github.ref, 'refs/tags/') || startsWith(github.ref, 'refs/tags/v') }}
    runs-on: ubuntu-latest
    env:
      GITEA_NPM_TOKEN: ${{ secrets.GITEA_NPM_TOKEN }}
    outputs:
      image_tag: ${{ steps.meta.outputs.tags }}
      image_version: ${{ steps.meta.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.source_sha || github.sha }}

      - name: Validate service tag
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        run: |
          TAG="${{ github.ref_name }}"
          if [[ ! "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Invalid production service tag: $TAG"
            echo "Expected a stable semver tag like v1.23.0."
            exit 1
          fi

      - name: Setup pnpm
        if: ${{ !inputs.skip_preflight }}
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        if: ${{ !inputs.skip_preflight }}
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Configure Talesofai npm auth
        if: ${{ !inputs.skip_preflight }}
        run: |
          if [ -z "$GITEA_NPM_TOKEN" ]; then
            echo "GITEA_NPM_TOKEN not set; installing without private billing packages"
          fi
          if [ -n "$GITEA_NPM_TOKEN" ]; then
            printf '\n//git.talesofai.com/api/packages/talesofai/npm/:_authToken=%s\n' "$GITEA_NPM_TOKEN" >> ~/.npmrc
          fi

      - name: Install dependencies
        if: ${{ !inputs.skip_preflight }}
        run: pnpm install --frozen-lockfile

      - name: Build dependency closure
        if: ${{ !inputs.skip_preflight }}
        run: pnpm --filter @cohub/gateway... build

      - name: Lint
        if: ${{ !inputs.skip_preflight }}
        run: pnpm --filter @cohub/gateway lint

      - name: Typecheck
        if: ${{ !inputs.skip_preflight }}
        run: pnpm --filter @cohub/gateway typecheck

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.GITEA_USERNAME }}
          password: ${{ secrets.GITEA_PAT }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.NAMESPACE }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=main-{{sha}},enable=${{ github.ref == 'refs/heads/main' }}
            type=sha,format=short
            type=raw,value=${{ github.ref_name }},enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./apps/gateway/Dockerfile
          cache-from: type=gha,scope=gateway-build
          cache-to: type=gha,mode=max,scope=gateway-build
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64
          secrets: |
            gitea_npm_token=${{ secrets.GITEA_NPM_TOKEN }}

  deploy-dev:
    needs: build-and-push
    if: ${{ (inputs.skip_preflight || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main' }}
    runs-on: ubuntu-latest
    env:
      IMAGE_TAG: ${{ needs.build-and-push.outputs.image_version }}
    steps:
      - name: Setup kubectl
        uses: azure/setup-kubectl@v5
        with:
          version: 'v1.30.0'

      - name: Configure kubectl
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG_DEV }}" | base64 -d > ~/.kube/config

      - name: Deploy to Dev
        run: |
          kubectl set image statefulset/cohub-gateway-dev cohub-gateway-dev=${{ env.REGISTRY }}/${{ env.NAMESPACE }}/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }} -n cohub-dev

      - name: Verify deployment
        run: |
          kubectl rollout status statefulset/cohub-gateway-dev -n cohub-dev --timeout=300s

  deploy-prod:
    needs: build-and-push
    if: ${{ (inputs.skip_preflight || github.event_name == 'workflow_dispatch') && startsWith(github.ref, 'refs/tags/v') }}
    runs-on: ubuntu-latest
    steps:
      - name: Setup kubectl
        uses: azure/setup-kubectl@v5
        with:
          version: 'v1.30.0'

      - name: Configure kubectl
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG_PROD }}" | base64 -d > ~/.kube/config

      - name: Get tag
        id: get-tag
        run: |
          TAG="${{ github.ref_name }}"
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Deploy to Prod
        run: |
          TAG="${{ steps.get-tag.outputs.tag }}"
          # 更新 StatefulSet image
          kubectl set image statefulset/cohub-gateway cohub-gateway=git.talesofai.com/talesofai/cohub-gateway:$TAG -n cohub

      - name: Verify deployment
        run: |
          kubectl rollout status statefulset/cohub-gateway -n cohub --timeout=300s
