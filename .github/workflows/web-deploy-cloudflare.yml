name: Web Deploy to Cloudflare Workers

on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'
    paths:
      - 'apps/web/**'
      - 'packages/**'
  workflow_dispatch:
    inputs:
      environment:
        description: '部署环境'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - prod

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy:
    if: ${{ !startsWith(github.ref, 'refs/tags/') || startsWith(github.ref, 'refs/tags/v') }}
    runs-on: ubuntu-latest
    env:
      GITEA_NPM_TOKEN: ${{ secrets.GITEA_NPM_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Configure Talesofai npm auth
        run: |
          if [ -z "$GITEA_NPM_TOKEN" ]; then
            echo "Missing required CI secret: GITEA_NPM_TOKEN"
            exit 1
          fi
          printf '\n//git.talesofai.com/api/packages/talesofai/npm/:_authToken=%s\n' "$GITEA_NPM_TOKEN" >> ~/.npmrc

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Determine environment
        id: env
        run: |
          # Tag 触发 → prod（仅服务 tag，如 v1.23.0）
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            TAG="${{ github.ref_name }}"
            if [[ ! "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
              echo "Invalid production service tag: $TAG"
              echo "Expected a stable semver tag like v1.23.0."
              exit 1
            fi
            echo "env=prod" >> $GITHUB_OUTPUT
            echo "config=wrangler.prod.toml" >> $GITHUB_OUTPUT
          # 手动触发 → 根据用户选择
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" && "${{ github.event.inputs.environment }}" == "prod" ]]; then
            echo "env=prod" >> $GITHUB_OUTPUT
            echo "config=wrangler.prod.toml" >> $GITHUB_OUTPUT
          # Push main → dev
          else
            echo "env=dev" >> $GITHUB_OUTPUT
            echo "config=wrangler.toml" >> $GITHUB_OUTPUT
          fi

      - name: Setup web environment
        working-directory: apps/web
        run: |
          echo "PUBLIC_COHUB_ENV=${{ steps.env.outputs.env }}" > .env
          if [[ "${{ steps.env.outputs.env }}" == "prod" ]]; then
            echo "PUBLIC_API_ORIGIN=https://api.cohub.run" >> .env
            echo "PUBLIC_GATEWAY_ORIGIN=https://gateway.cohub.run" >> .env
            echo "PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_51SYJ1w0hIk0rCwosX4Qxw8y6agSRruF9RDYafMyM2dlcJmvn9nNsqYdJyCVE9OqnFEgLv7BdQn5Yc6TiE5jR85NC00ND4wtitx" >> .env
          else
            echo "PUBLIC_API_ORIGIN=https://api-dev.cohub.run" >> .env
            echo "PUBLIC_GATEWAY_ORIGIN=https://gateway-dev.cohub.run" >> .env
            echo "PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_51SYJ1w0hIk0rCwosusvVnsEoK7jupdIumnDI0cECxL1b8IvY9SIh9sIaKXhpslWiHeU0ZjIiGPHMpmna9xMONlJY00vL1MpLvM" >> .env
          fi

      - name: Lint
        run: pnpm lint
        working-directory: apps/web

      - name: Typecheck
        run: pnpm typecheck
        working-directory: apps/web

      - name: Build
        run: pnpm build
        working-directory: apps/web

      - name: Deploy to Cloudflare
        run: pnpm run deploy --config ${{ steps.env.outputs.config }}
        working-directory: apps/web
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
